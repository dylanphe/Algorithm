        {
                                        tempt += *(curr.begin());
                                        curr.erase(curr.begin());
                                        cnt = curr.size();
                                    }